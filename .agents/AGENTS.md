## Git

* Always use the GitHub CLI instead of querying the GitHub API directly.
  * Can be invoked with `gh` and `gh --help` for documentation.
  * Continue to use `git` for standard Git operations.

## TypeScript

- Do not sprinkle TypeScript use type params unless necessary.
- Using `as` is forbidden in production code.
